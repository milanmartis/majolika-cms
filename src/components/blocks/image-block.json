{
    "collectionName": "components_blocks_image_blocks",
    "info": {
      "displayName": "Image Block"
    },
    "options": {},
    "attributes": {
      "media": {
        "type": "media",
        "multiple": false,
        "required": true
      },
      "caption": {
        "type": "string"
      },
      "columns": {
        "type": "enumeration",
        "enum": ["one", "two"],
        "default": "one",
        "required": false
      },
      "alignment": {
        "type": "enumeration",
        "enum": ["left","center","right"],
        "default": "center"
      }
    }
  }